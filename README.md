# Retro ASCII Character Creator

A text-based UI generator for an RPG character sheet, built entirely with Python Day 1 concepts.

Future ImprovementsL Use the len() function and formatted strings to mathematically align the right-side border perfectly, regardless of the user's name length.

Version 2 Ideas: Add random stat generation once the random module and integers are learned.
